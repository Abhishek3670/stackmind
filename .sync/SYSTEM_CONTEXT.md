# System Context — stackmind

> **Read this file at the start of every session.**
> Contains environment-specific information all agents need.

---

## Environment

| Property | Value |
|----------|-------|
| **Project** | stackmind |
| **Workspace** | `W:/Aatish/Stuff/stackmind` |
| **Sync Bus** | `W:/Aatish/Stuff/stackmind/.sync` |
| **OS** | Windows 10/11 |
| **Shell** | PowerShell |

---

## Shell Notes

### Windows (PowerShell)
- `&&` does NOT work — use `;` to chain commands
- Use `\` for path separators in commands
- Activate venv: `.\.venv\Scripts\Activate.ps1`

### Linux/macOS (Bash)
- `&&` works for command chaining
- Use `/` for path separators
- Activate venv: `source .venv/bin/activate`

---

## Repository Structure

This project uses **two git repositories**:

1. **Project repo** (`W:/Aatish/Stuff/stackmind`)
   - Contains application code
   - Only Local-LLM commits (D007)

2. **Sync repo** (`W:/Aatish/Stuff/stackmind/.sync`)
   - Contains agent state and communication
   - All agents commit

---

## Runtime Version

Check `RUNTIME_VERSION` for:
- Current runtime version
- Schema versions
- Minimum CLI version

Use `stackmind doctor` to validate runtime health.

---

## Key Files

| File | Purpose |
|------|---------|
| `AGENTS.md` | Universal agent rules (project root) |
| `runtime/TREE.yaml` | Team state index |
| `runtime/boot/*.yaml` | Agent boot snapshots |
| `work-orders/INDEX.yaml` | Work order index |
| `PROTOCOL_DIGEST.md` | Protocol rules |

---

**Generated by stackmind v1.0.0**
